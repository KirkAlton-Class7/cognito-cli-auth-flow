# ============================================================
# ENVIRONMENT TEMPLATE
# ============================================================
# Unused Token Detector Lab Environment Template
# Copy this file to .env, then update .env as you create resources.
# Do not commit .env with real account IDs, secrets, or lab-specific values.
# ------------------------------------------------------------

# ==============================================================
# INPUTS - PLANNED VALUES
# ==============================================================
REPO_ROOT=""
AWS_ACCOUNT_ID=""
AWS_REGION="us-east-1"
PROJECT_NAME="chewbacca-auth-rest"

JEDI_FUNCTION="${PROJECT_NAME}-jedi-python"
SITH_FUNCTION="${PROJECT_NAME}-sith-node"
LAMBDA_ROLE_NAME="${PROJECT_NAME}-lambda-basic-role"
API_NAME="${PROJECT_NAME}-api"
USER_POOL_NAME="${PROJECT_NAME}-users"

TOKEN_TABLE_NAME="${PROJECT_NAME}-jedi-token-holocron"
TOKEN_DETECTOR_FUNCTION="${PROJECT_NAME}-unused-token-detector"
TOKEN_SCAN_SCHEDULE="${PROJECT_NAME}-unused-token-check"
TOKEN_ALERT_TOPIC="${PROJECT_NAME}-auth-alerts"

TOKEN_ALERT_FILTER_NAME="${PROJECT_NAME}-unused-token-filter"
TOKEN_ALERT_METRIC_NAMESPACE="${PROJECT_NAME}/auth-security"
TOKEN_ALERT_METRIC_NAME="UnusedTokenAlertCount"
TOKEN_ALERT_ALARM_NAME="${PROJECT_NAME}-unused-token-alarm"

API_BASE="https://<API_ID>.execute-api.${AWS_REGION}.amazonaws.com/prod"

# ==============================================================
# OUTPUTS - POST-DEPLOYMENT VALUES
# ==============================================================
LAMBDA_ROLE_ARN=""
TOKEN_TABLE_ARN="arn:aws:dynamodb:${AWS_REGION}:${AWS_ACCOUNT_ID}:table/${TOKEN_TABLE_NAME}"
TOKEN_DETECTOR_FUNCTION_ARN=""
TOKEN_ALERT_TOPIC_ARN=""
TOKEN_SCAN_SCHEDULE_ARN=""
REST_API_ID=""
HTTP_API_ID=""
USER_POOL_ID=""
